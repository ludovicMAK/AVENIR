# Repository driver
DATA_SOURCE=postgres

# Database
DB_HOST=localhost
DB_PORT=5433
POSTGRES_DB=avenir
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin

# (Use this when you have a full Postgres connection URL availabl, so the pool can skip the DB_HOST/DB_PORT/... fallback.)
# DATABASE_URL= 

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Backend
BACKEND_PORT=8000
FRONTEND_URL=http://localhost:3000